# espix partition table — ESP32-S3, 8MB flash.
#
# Layout fills the flash exactly; last byte is 0x7FFFFF (ends at 0x800000).
# The partition table itself lives at 0x8000 (CONFIG_PARTITION_TABLE_OFFSET).
#
# The same shape as the 16MB table: a small loader (ota_0) and one large kernel
# slot (ota_1). Kernels are files on the rootfs and the loader installs them, so
# the kernel gets the space a second A/B slot would have used.
#
# Everything before `storage` is identical to the 16MB table -- loader at
# 0x20000, kernel at 0x90000, coredump at 0x410000 -- so one kernel and one
# first-flash image serve both sizes. Only `storage` differs because it is
# whatever is left. Adopting this table moves `storage` (the old factory layout
# had it at 0x320000), so an 8MB board reflashes the rootfs once.
#
# Name,     Type, SubType,  Offset,    Size,      Flags
nvs,        data, nvs,      0x9000,    0x6000,
otadata,    data, ota,      0xf000,    0x2000,
phy_init,   data, phy,      0x11000,   0x1000,
# ota_0 is the loader, which runs first; ota_1 is the kernel it selects.
ota_0,      app,  ota_0,    0x20000,   0x70000,
ota_1,      app,  ota_1,    0x90000,   0x380000,
coredump,   data, coredump, 0x410000,  0x10000,
storage,    data, littlefs, 0x420000,  0x3E0000,
