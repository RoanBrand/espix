# espix partition table — ESP32-S3, 8MB flash.
#
# Layout fills the flash exactly; last byte is 0x7FFFFF (ends at 0x800000).
# The partition table itself lives at 0x8000 (CONFIG_PARTITION_TABLE_OFFSET).
#
# The same shape as the 16MB table: one large kernel slot (ota_0) and a small
# loader (ota_1). Kernels are files on the rootfs and the loader installs them,
# so the kernel gets the space a second A/B slot would have used; here that is
# 2MB, and the rootfs keeps 5.5MB.
#
# Adopting this table moves `storage` -- the old factory layout had it at
# 0x320000 -- so an 8MB board reflashes the rootfs once. coredump and nvs are
# where they always were, and both app offsets are 64KB-aligned.
#
# Name,     Type, SubType,  Offset,    Size,      Flags
nvs,        data, nvs,      0x9000,    0x6000,
otadata,    data, ota,      0xf000,    0x2000,
phy_init,   data, phy,      0x11000,   0x1000,
ota_0,      app,  ota_0,    0x20000,   0x200000,
ota_1,      app,  ota_1,    0x220000,  0x50000,
coredump,   data, coredump, 0x270000,  0x10000,
storage,    data, littlefs, 0x280000,  0x580000,
