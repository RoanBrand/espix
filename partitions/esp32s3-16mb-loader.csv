# espix partition table — ESP32-S3, 16MB flash. Single kernel slot + loader.
#
# Layout fills the flash exactly; last byte is 0xFFFFFF (ends at 0x1000000).
#
# One big kernel slot (ota_0) and a small loader (ota_1), instead of two equal
# A/B slots. The kernel gets the space two slots used to share; the loader is
# the app that puts a kernel file into ota_0 and selects it, and is also what
# the bootloader falls back to when a new kernel does not confirm. Kernels
# themselves live as files on the rootfs, so a version is a file, not a slot.
#
# nvs, coredump and storage are where they have always been: adopting this
# table does not reformat the rootfs. Both app offsets are 64KB-aligned.
#
# Name,     Type, SubType,  Offset,    Size,      Flags
nvs,        data, nvs,      0x9000,    0x6000,
otadata,    data, ota,      0xf000,    0x2000,
phy_init,   data, phy,      0x11000,   0x1000,
ota_0,      app,  ota_0,    0x20000,   0x380000,
ota_1,      app,  ota_1,    0x3A0000,  0x70000,
coredump,   data, coredump, 0x410000,  0x10000,
storage,    data, littlefs, 0x420000,  0xBE0000,
