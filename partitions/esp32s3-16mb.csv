# espix partition table — ESP32-S3, 16MB flash. Single kernel slot + loader.
#
# Layout fills the flash exactly; last byte is 0xFFFFFF (ends at 0x1000000).
#
# A small loader (ota_0) and one big kernel slot (ota_1), instead of two equal
# A/B slots. Blank otadata boots the loader, which puts a kernel file into ota_1
# and selects it; the kernel gets the space two slots used to share. The loader
# is also what the bootloader falls back to when a new kernel does not confirm.
# Kernels themselves live as files on the rootfs, so a version is a file, not a
# slot.
#
# nvs, coredump and storage are where they have always been: adopting this
# table does not reformat the rootfs. Both app offsets are 64KB-aligned.
#
# Name,     Type, SubType,  Offset,    Size,      Flags
nvs,        data, nvs,      0x9000,    0x6000,
otadata,    data, ota,      0xf000,    0x2000,
phy_init,   data, phy,      0x11000,   0x1000,
# ota_0 is the loader, which runs first; ota_1 is the kernel it selects.
ota_0,      app,  ota_0,    0x20000,   0x70000,
ota_1,      app,  ota_1,    0x90000,   0x380000,
coredump,   data, coredump, 0x410000,  0x10000,
storage,    data, littlefs, 0x420000,  0xBE0000,
