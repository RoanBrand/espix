dependencies:
  idf:
    version: ">=6.1"

  # project_elf() and the app-side link script.
  espressif/elf_loader: "~1.3.3"

  # Arduino as a library for this app only. espix does not depend on it.
  espressif/arduino-esp32: "*"
